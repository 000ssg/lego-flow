# Messaging Compliance + Interop — PROGRESS

Live checklist. Keep straight: mark `[x]` when the sub-task is done and verified.
(See `PLAN.md` for what each task means, `ISSUES.md` for blockers, `DECISIONS.md` for trade-offs.)

## Phase 0 — Repo cleanup + planning
- [x] Recon: compliance map for all 7 messaging modules
- [x] Plan docs (`doc/plans/messaging/PLAN.md`) + tracking files
- [x] Delete `messaging/amqp-091/` build debris
- [x] Commit Phase 0 (`b0489992`)

## Phase 1 — Reference-pattern audit + baseline
- [x] `audit.md` full compliance map + reference-pattern definition
- [x] Verify baseline modules (mqtt/stomp/amqp/wamp) satisfy all 6 reference criteria
- [x] Commit Phase 1 (`6a588918`)

## Phase 2 — NATS
- [x] `NatsTransport` SPI (byte-level, `receiveWithTimeout`; timeout ≠ EOF)
- [x] `InMemoryNatsTransport` (`createPair()`)
- [x] `PipelineNatsTransport` (DataChannel ring + outbound queue, selector-thread driven)
- [x] `NatsClient` off raw Socket (transport-injected; handshake over `NatsTransport`)
- [x] `NatsServer` off ServerSocket (headless; `handleConnection(NatsTransport)` seam)
- [x] Service layer drives I/O (`NatsService`/`NatsServerService` + manager; zero sockets in core)
- [x] Codec reassembly verified/fixed (reassembly tests green in the 343-test run)
- [x] Loopback test files migrated to in-memory seam (5 files); service integration test added
- [x] Demos + interop compile against the refactored API (in-memory seam for demos; real-TCP service layer for interop)
- [x] Full NATS suite re-run green + coverage ≥80% (JaCoCo) — 343 tests, 0 failures; instruction coverage 85.1%
- [x] Commit Phase 2

## Phase 3 — XMPP
- [x] `XmppTransport` SPI
- [x] `InMemoryXmppTransport`
- [x] `PipelineXmppTransport`
- [x] `XmppServer` off ServerSocket
- [x] Service layer drives I/O
- [x] Codec reassembly verified/fixed
- [x] Unit tests ≥80%
- [x] Commit Phase 3 (`c5df955b`)

## Phase 4 — KAFKA
- [x] `KafkaTransport` SPI
- [x] `InMemoryKafkaTransport`
- [x] `PipelineKafkaTransport`
- [x] `KafkaConnection` off SocketChannel
- [x] `KafkaBroker` off accept loop
- [x] Codec reassembly verified/fixed (head-of-stream buffer)
- [x] Unit tests ≥80% (client path, then broker path)
- [x] Commit Phase 4 (`c71fcde7`)

## Phase 5 — Interop structure: 4 groups
- [x] JUnit tags for 4 groups (core: 5 classes / 15 tests — was 19 before the 2026-09-22 wire-capture removal; rest: 14 classes / 186 tests; kafka+wamp: 0, land in Phase 6)
- [x] Tag existing interop classes (frozens composition)
- [x] `interop-tests/pom.xml` tag selection (`-Dinterop.group=<g>`; `failIfNoTests` via `interop-group` profile) — verified: core 19/19 green, wamp empty OK with `-Dinterop.failIfNoTests=false`, typo group fails the build
- [x] CI: 3 active concurrent jobs (messaging-core, kafka, wamp) + rest job disabled; wire-capture reference clients removed 2026-09-22 (record-only, zero assertions — see D12/D14)
- [x] docker-compose: Kafka broker (cp-kafka KRaft, healthy) + WAMP router (crossbar, host 8081, healthy); rabbitmq pinned to 3.13 (reproducible interop — 4.x changed STOMP/AMQP1 plugin behavior), artemis pinned to 2.57.0-alpine
- [x] Commit Phase 5 (`e21cdeb1`)

## Phase 6a — Kafka codec version accuracy + structure (NEW 2026-09-23)
- [x] Spec artifact set complete in tree (`messaging/kafka/doc/spec/` — 74 JSONs, apache/kafka 3.6.1; 6 missing fetched 2026-09-23)
- [x] WIP diff preserved (`kafka-wip-2026-09-23.patch`) + broken source files reverted to green baseline
- [x] Live-broker version matrix captured (cp-kafka 7.6.1, localhost:9092) — matches 3.6.1 schema; 1 documented exception (OffsetCommit v9 spec / v8 broker)
- [x] Plan + 210-row version sub-task matrix written (`PHASE6A_KAFKA_CODEC_VERSIONS.md`)
- [x] Foundation commit (spec set + plan docs + `ApiKey.java` range fixes) — `5f4acacb` (2026-09-23)
- [x] Façade split by sub-category (`KafkaCodecPrimitives` + delegation, zero behavior change) — first landed with Negotiation/Auth v0 (`3f42231e`): primitives + 3 per-API classes for the Negotiation/Auth sub-category; remaining sub-categories follow their own sub-task rows
- [ ] ApiVersions negotiation + version registry in `KafkaConnection`
- [x] Negotiation/Auth sub-category (9 version sub-tasks) — v0 ✓ (`3f42231e`); v1 ✓ (`2f2d5ba8`); v2 ✓ (`bf084112` ApiVersions + `8eb1858f` SaslAuthenticate — flexible encoding); v3 ✓ (ApiVersions — flexible encoding + new request/response fields, all 9 rows done)
- [ ] Record I/O (35) — in progress: Produce v0 ✓ (spec-correct layout, dedicated `ProduceCodec`); Produce v1 ✓ (`3e6d9a3c` — request byte-identical to v0 per spec, response + trailing ThrottleTimeMs); Produce v2 ✓ (`6dc997a8` — request still v0-identical, response partition + LogAppendTimeMs); remaining 32 rows + Admin (35), Transactions (24), Consumer Groups (63), Metadata/Cluster (44) pending
- [ ] Per-row status in the matrix as commits land

## Phase 6 — Kafka + WAMP composite interop
- [ ] Kafka: trivial → produce/fetch → multi-partition streaming → transactions (gated on Phase 6a)
- [ ] WAMP: trivial → multi-realm → distributed procedure executors → sharding
- [ ] Green vs real brokers
- [ ] Commit Phase 6

## Phase 7 — Guidelines + final docs
- [ ] Refine `doc/PROTOCOL-GUIDELINES.md` (messaging reference pattern section)
- [ ] Module docs for nats/xmpp/kafka
- [ ] Root `doc/REQUIREMENTS.md` append
- [ ] Full verification (unit + coverage + interop groups 1–3)
- [ ] Commit Phase 7

## Log
- 2026-09-24 — Record I/O sub-category row 3 (Produce v2, API 0): spec-verified from the 3.6.1 JSONs — `LogAppendTimeMs:int64` at versions `2+` in the response partition (after `BaseOffset`; spec default -1, ignorable), trailing `ThrottleTimeMs` retained from v1; the request v2 is unchanged vs v0 (`TransactionalId` only arrives in v3 — so v0/v1/v2 requests all share the v0 methods, byte-identical). New `encodeResponseV2`/`decodeResponseV2` in `ProduceCodec` (v1 response body + per-partition int64 logAppendTime; partition width 14→22 bytes). Out of scope for this row: `LogStartOffset` (v5+), `RecordErrors`/`ErrorMessage` (v8+), flexible encoding (v9+) — next row is v3 (`+ TransactionalId:string[3+](null:3+)` on the request — the first framing change since v0). `ProduceCodecTest` 16→20: v2 request byte-identity vs v0 + decode (transactionalId null); v2 response round-trip (carried 1711000000L and default -1 per partition); exact 41-byte v2 layout asserting every offset incl. logAppendTime after BaseOffset; multi-topic v2 round-trip; the four dispatch tests that asserted v2 throws are re-pinned to v3 (request encode/decode + response encode/decode all covered). Full module suite **479 green** (was 475). Matrix row 3 marked ✓ `6dc997a8`.
- 2026-09-24 — Record I/O sub-category row 2 (Produce v1, API 0): verified from the 3.6.1 spec JSONs — "Version 1 and 2 are the same as version 0" for the request, so v1 request dispatches fall through to the shared v0 methods (byte-identical, mechanism (a) of plan §3, same pattern as ApiVersions v1/v2); the response gains a trailing `ThrottleTimeMs` (int32) after the TopicData array → dedicated `encodeResponseV1`/`decodeResponseV1` in `ProduceCodec` (partition layout still v0 — `LogAppendTimeMs` is v2+, carried value discarded, decoded as -1). `ProduceCodecTest` 11→16: v1 request byte-identity vs v0 + decode (incl. null→null transactionalId, added v3), v1 null-records round-trip, v1 response round-trip (throttle 250 carried, logAppend defaulted -1), exact 33-byte v1 response layout asserting every field offset (trailing throttle after the array, zero remaining), multi-topic v1 round-trip; the four dispatch tests that asserted v1 throws are re-pinned to v2 (next unimplemented version). Full module suite **475 green** (was 470; +5 new tests, 4 dispatch tests re-pinned v1→v2 1:1). Matrix row 2 marked ✓ `3e6d9a3c`.
- 2026-09-24 — Record I/O sub-category started; Produce v0 (API 0, first row of 35): spec-first re-derivation per plan §3 — the committed v0 layout (from the green 416-test baseline) was a **layout bug**: `encodeProduceRequest` unconditionally wrote a leading nullable `transactionalId` (a **v3+** field), i.e. a v3-shaped body under a v0 frame that a real broker misparses, and `encodeProduceResponse` wrote per-partition `logAppendTimeMs` (v2+) and trailing `throttleTimeMs` (v1+). Split the Produce API into a dedicated `ProduceCodec` (facade `KafkaCodec.encodeProduceRequest/decodeProduceRequest/encodeProduceResponse/decodeProduceResponse` now delegate at the in-house pinned v0 — same pattern as `SaslHandshakeCodec`/`ApiVersionsCodec`/`SaslAuthenticateCodec`; `BufferPool` + `KafkaCodecPrimitives` reuse, no new primitives needed — v0 is all fixed-width). v0 request = `Acks(int16) + TimeoutMs(int32) + TopicData[](Name(string16) + PartitionData[](Index(int32) + Records(nullable bytes)))`; v0 response = `TopicData[](Name(string16) + PartitionResponse[](Index(int32) + ErrorCode(int16) + BaseOffset(int64)))`. Model keeps `transactionalId` (v3+) / `logAppendTimeMs` (v2+) / `throttleTimeMs` (v1+); at v0 they are never written and decoded as null/-1/0. v1+ throw `CodecNotImplementedException` (no silent fall-through). New `ProduceCodecTest` (11 tests: exact-byte v0 request — 25-byte null-records and 32-byte records cases verifying every field offset incl. no leading transactionalId; exact-byte v0 response — 25 bytes, no logAppend/throttle; round-trips incl. multi-topic/mixed records presence; error-code partition; v1/v3 dispatch throws). `KafkaCodecTest` Produce section re-pinned to v0 (85→85, 1:1 replacement): the old `testProduceRequest` asserted the buggy `transactionalId` round-trip and is now a façade-level v0 round-trip (byte-level conformance moved to the per-sub-category mirror per plan §3); `testProduceResponse` now asserts the v0 throttle-discard. Full module suite **470 green** (was 459; +11 from `ProduceCodecTest`). Interop-tests recompile clean against the delegating facade. Out of scope, tracked: Produce v1 (throttle) next row per the one-version-per-sub-task rule; the interop `KafkaKRaftInteropTest` (committed 357e7e08) still pins Produce v3 in its header — it is gated on the v3 row + the whole Record I/O + Admin paths, so it stays compile-only until then.
- 2026-09-24 — Phase 6 scaffolding committed (pre-gating): `KafkaKRaftInteropTest` (192 lines, `@Tag("interop-kafka")`) + `interop-tests/pom.xml` (kafka/wamp test deps, `interop.kafka.*`/`interop.wamp.*` properties) land as a compile-only WIP ahead of the Record I/O + Admin sub-tasks it exercises (plan §4: interop is the final verifier, not the development driver). Verified: forced recompile green after a stale 09-23 class in `target/` (a naive `test-compile` said "nothing to compile" — `rm -rf` the class and recompile when a fresh file lands). Next: Record I/O sub-category, Produce v0 first (the committed v0 layout predates the spec-first reset — re-verify against `ProduceRequest.json` before trusting it).
- 2026-09-24 — Negotiation/Auth v3 (ApiVersions v3, one version, no mixing — last row of the sub-category): v3 is the **flexible-encoding** version and the first with new request/response fields. Per the 3.6.1 spec + generated sources (`ApiVersionsRequestData`/`ApiVersionsResponseData`), the request adds `ClientSoftwareName`/`ClientSoftwareVersion` as **non-nullable compact strings** (null→empty) then a trailing tagged section (count 0 — no request tags); the response base body is `ErrorCode` + a **compact** ApiKeys array (each `ApiVersion` = 3×int16 + per-element tagged trailer `varint(0)`) + `ThrottleTimeMs`, followed by a tagged section with a **leading count varint** (no trailing sentinel) and, when present, tag 0 = `SupportedFeatures` (implicit compact array), tag 1 = `FinalizedFeaturesEpoch` (int64), tag 2 = `FinalizedFeatures` (implicit compact array, **maxVersionLevel before minVersionLevel** — a 3.6.1 wire quirk), tag 3 = `ZkMigrationReady` (bool). Feature version fields are **int16**, not int32. Mechanism per plan §3: framing-divergent version → dedicated `encodeRequestV3`/`decodeRequestV3`/`encodeResponseV3`/`decodeResponseV3` in `ApiVersionsCodec` (reusing the `KafkaCodecPrimitives` varint/compact-string helpers added in the SaslAuthenticate v2 row; the tagged section is decoded via a `slice()` window so absent tags fall back to the record defaults — empty lists, `-1L` epoch, `false` flag — which re-encode to byte-identical output because the encoder omits absent tags). `ApiVersionsResponse` record gains the four v3 components with the existing convenience constructors preserved. New tests: 6 ApiVersions v3 (exact 13-byte request + null→empty round-trip; exact 15-byte minimal response; absent-features decode→default→byte-identical re-encode; exact 54-byte all-four-tags vector verifying every offset incl. max-before-min + int16 + implicit-list `varint(size+1)` tag sizes; unknown-tag skip advancing to the exact boundary). `NegotiationAuthCodecTest` ApiVersions nested 7→12 (v3 stub removed). Full module suite **459 green** (was 454). Out of scope, tracked (unchanged): facade/broker version-echo wiring + flexible response headers — still the deferred "ApiVersions negotiation + version registry in `KafkaConnection`" row (facade dispatches at v0; the v3 paths are unit-tested via the `short version` parameter). Next sub-category per the plan matrix.
- 2026-09-23 — Negotiation/Auth v2 row 2 of 2 (SaslAuthenticate v2, one version, no mixing): v2 is the **flexible-encoding** version — same field list as v1, but all length fields become varints/compact and the request header becomes flexible (apiKey|0x8000 + varint correlationId + compact nullable clientId). Implemented per 3.6.1 spec: added unsigned varint + compact string/bytes primitives to `KafkaCodecPrimitives` (null↔empty ambiguity of compact nullable bytes is inherent to the wire format — null and empty both encode as varint 1); dedicated `encodeRequestV2`/`decodeRequestV2`/`encodeResponseV2`/`decodeResponseV2` in `SaslAuthenticateCodec` (fixed-width int16/int64 unchanged by flexible encoding); additive flexible frame-level header codec `encodeRequest(header, payload, flexible=true)` + `decodeRequestHeaderFlexible` in `KafkaCodec` (legacy `decodeRequestHeader` untouched — version-echo wiring is the separate deferred row). Mechanism per plan §3: framing-divergent version → dedicated V2 methods. Fixed a real framing bug the new exact-byte tests caught: the flexible encoder's `headerSize` over-allocated the correlationId varint as 5 bytes → declared frame length mismatched actual bytes (would corrupt interop); now uses actual varint size via `varintSize()`. New tests: 6 SaslAuthenticate v2 (exact 11-byte request, exact 12/19-byte responses, null-field round-trips, multi-byte varint) + 5 FlexibleRequestFrame (flexible header exact bytes, null clientId, round-trips, legacy path regression guard). Also: `dump_matrix.py` Δ column now flags the first flexible version (`→ flexible encoding`) — same class of tracking gap as the earlier request-only Δ bug (v2 had been marked "unchanged" despite a framing change); matrix regenerated (210 rows, 35 flexible markers, 8 done rows re-marked). Full module suite **454 green** (was 443; `NegotiationAuthCodecTest` 19→32). Out of scope, tracked: flexible **response** headers (v1 response header with varint correlationId) and broker version-echo wiring — both part of the deferred "ApiVersions negotiation + version registry in `KafkaConnection`" row. Next sub-task: ApiVersions v3 (flexible + 4 new request fields + tagged fields in response) — last row of the Negotiation/Auth sub-category.
- 2026-09-23 — Negotiation/Auth v2 sub-task, row 1 of 2 (ApiVersions v2, one version, no mixing): verified the v2 layout from the 3.6.1 spec JSON — request byte-identical (no request fields until v3) and response byte-identical to v1 (throttle semantics changed — brokers may send the response before throttling on quota violation — but no field delta in the 3.6.1 schema). Mechanism per plan §3: byte-identical version → shared v1 methods (`case 2:` fall-through in all four dispatch methods, no duplicated layout code). New unit tests in `NegotiationAuthCodecTest` (ApiVersions: 5→7 tests — v2 request byte-identity, v2 response round-trip with byte-identical-to-v1 assertion; v3 stub added as the next sub-task). Out of scope, tracked: ApiVersions v3 (flexible encoding + tagged fields) and SaslAuthenticate v2 (flexible encoding) — both require the flexible-length/type primitives, a separate sub-task per row.
- 2026-09-23 — Negotiation/Auth v1 sub-task (3 rows, one version, no mixing): implemented v1 in the three per-API codec classes per the 3.6.1 spec layouts — SaslHandshake v1 is byte-identical to v0 in the schema (dispatch falls through to the v0 methods), ApiVersions v1 adds `ThrottleTimeMs` (int32) to the **response** after the ApiKeys array (request byte-identical), SaslAuthenticate v1 adds `SessionLifetimeMs` (int64) to the **response** after authBytes (request byte-identical). Mechanism: dedicated `encodeResponseV1`/`decodeResponseV1` for the two layout-divergent responses, shared v0 methods for byte-identical cases. New unit tests in `NegotiationAuthCodecTest` with exact-byte layout assertions (v1 stubs → round-trip; 14→19 tests). Full module suite **441 green** (was 437). Matrix rows for the three v1 versions marked ✓; `dump_matrix.py` now unions request+response deltas (v1 rows show the response-side fields). Out of scope, tracked: broker/facade version-echo wiring (facade still dispatches at v0 — the v1 paths are unit-tested via the `short version` parameter; wiring the negotiated version through `KafkaConnection` → handler → `encodeResponse` is the "ApiVersions negotiation + version registry in `KafkaConnection`" row). Next sub-task: ApiVersions v2 (response unchanged vs v1 → shared v1 response path) per the same one-version-per-sub-task rule.
- 2026-09-23 — Phase 6a inserted (user direction, after Phase 5): the Phase 6 WIP was **red** (4 unit-test errors: `KafkaCodecTest.testCreateTopicsRequest` BufferUnderflow + 3 admin-client "Connection closed") and its "fix" (CreateTopics v0 `replicationFactor` int16→int32, `Configs` + `timeoutMs` deleted, Produce v0→v3 bump in `KafkaProducer`) was guessed from live-broker behavior, **not** from the schema — the committed v0 layout was equally wrong (missing the spec's `Assignments` array). WIP diff preserved verbatim in `kafka-wip-2026-09-23.patch`, broken sources reverted to the green 416-test baseline. New ground rules: (1) spec-first — `messaging/kafka/doc/spec/message/*.json` (apache/kafka 3.6.1, now complete: 74 files) is the only layout source; broker observation is a check, never a source; (2) one API version per sub-task, never mixed; (3) unit tests before interop — interop is the final verifier, not the development driver; (4) codec split by API sub-category into small classes (210 version sub-tasks tracked row-by-row in `PHASE6A_KAFKA_CODEC_VERSIONS.md`); (5) `ApiKey.java` ranges stale for 5 APIs (Fetch 13→15, ListOffsets 7→8, LeaderAndIsr 5→7, StopReplica 3→4, UpdateMetadata 7→8) — verified against the live broker matrix. Plan goals in `PLAN.md` untouched; Phase 6 (interop) is gated on 6a, not replaced.
- 2026-09-18 — Plan written; compliance map done (kafka/nats/xmpp violate; mqtt/stomp/amqp/wamp baseline).
- 2026-09-19 — Phase 1 committed (`6a588918`). Phase 2 main-code refactor: headless core + `PipelineNatsTransport` + manager-driven services (no sockets in core/protocol; earlier raw-socket `SocketNatsTransport`/`NatsAcceptor` draft removed, D7 corrected). Test migration to in-memory seam + TCP integration test in progress.
- 2026-09-20 — Phase 2 test migration complete: all 5 legacy loopback test files on the in-memory seam; fixed an `InMemoryNatsTransport` race where queued `-ERR` was dropped on close (D8); full NATS module suite green (343 tests). Discovered the `demos` module + NATS interop test still referenced the removed socket API (a stale `~/.m2` jar had masked the break) — migrated demos to the in-memory seam and interop to the real-TCP service layer (D9).
- 2026-09-20 — Phase 2 complete + committed: demos/interop compile verified; full suite re-run 343/343 green; JaCoCo instruction coverage 85.1% (≥80% gate); module docs (README/ARCHITECTURE/COMPLIANCE/REQUIREMENTS) updated to the headless architecture; root `doc/REQUIREMENTS.md` entry appended. Next: Phase 3 (XMPP).
- 2026-09-21 — Phase 3 (XMPP) complete + committed (`c5df955b`, 22 files, +1762/−145): `XmppTransport` SPI + `InMemoryXmppTransport`/`PipelineXmppTransport`; `XmppServer` off `ServerSocket` (per-connection `handleConnection(XmppTransport)` + non-blocking read loop); `XmppClient` transport-injected ctor + virtual-thread read loop + `flushOutbound` (stream stanza-listener registration fixed, outbound queue made concurrent); service layer (`XmppClientService`/`XmppServerService` + channel handlers) mirrors NATS via `SelectableChannelManager`; codec reassembly verified at transport level (partial reads requeued); full module suite **283 green** (was 268); JaCoCo instruction coverage **81.7%** (≥80% gate); demos + interop compile clean (legacy in-memory API preserved, demo suite 30/30); module docs updated to the headless architecture. Next: Phase 4 (Kafka).
- 2026-09-22 — Phase 4 (Kafka) complete + committed (`c71fcde7`, 33 files, +2294/−473): `KafkaTransport` SPI + `InMemoryKafkaTransport`/`PipelineKafkaTransport`; `KafkaBroker` off the accept loop (per-connection `handleConnection(KafkaTransport)` + virtual-thread read loop); `KafkaConnection` + the three public clients transport-injected (D11 — headless, no host/port ctor); service layer (`KafkaBrokerService`/`KafkaClientService` + channel handlers) mirrors NATS via `SelectableChannelManager`; `BrokerCluster` drops its dead `throws IOException`. **Bug found + fixed during reassembly testing**: `InMemoryKafkaTransport` re-queued a partially-read buffer's tail at the *back* of the queue, rotating the byte stream when two sends were in flight — a split frame no longer reassembled in order (3 broker wire-reassembly tests caught it); fixed by holding the partial buffer at the stream head (`headBuffer`). Full module suite **416 green** (was 399); JaCoCo instruction coverage **91.9%** / branch **80.4%** / line **91.7%** (≥80% gate); demos migrated to the dual-backend seam (in-memory for in-house broker, TCP via `KafkaClientService` for external brokers) + interop compile clean; headless audit: zero `java.net` socket imports in broker/codec/protocol/common/record/transport. Module docs (README/ARCHITECTURE/COMPLIANCE/REQUIREMENTS) updated to the headless architecture; root `doc/REQUIREMENTS.md` entry appended. Next: Phase 5 (interop structure).
- 2026-09-22 — Phase 5 (interop structure) complete + committed (`e21cdeb1`, 35 files, +482/−266): 4 JUnit tags + tag selection in `interop-tests/pom.xml` (`-Dinterop.group=<g>` → surefire `<groups>`; `interop-group` profile flips `failIfNoTests` on, override with `-Dinterop.failIfNoTests=false`); CI split into 3 active concurrent jobs (messaging-core / kafka / wamp) — `interop-rest` (14 existing classes, 186 tests) re-tagged but its CI job stays disabled (frozen decision; re-shuffling protocols across groups is a later session); docker-compose gains Kafka (cp-kafka 7.6.1, single-node KRaft, `CLUSTER_ID`) and Crossbar (host 8081 to avoid nginx 8080). **Wire-capture fixes found during verification:** (1) `Amqp10WireCaptureTest` used `guest`/`guest` — the Artemis entrypoint creates only the user from `ARTEMIS_USER=artemis`, so the captures were a silent SASL-retry loop (378 B) and the tests false-greened; fixed to `artemis`/`guest` → real 6–14 KB captures incl. transfer + disposition frames; (2) `rabbitmq:4-management` floated to 4.3 and rejects the `transient_nonexcl_queues` feature aiormq's auto-delete queues need → pinned to `rabbitmq:3.13-management`; (3) `amqp_capture_scenario.py` used the removed aiormq 7-style `channel.consume()` async-iter API → rewritten to the 6.x callback API (`basic_consume`/`basic_get`, `basic_ack`); (4) CI core job now copies the Artemis CLI out of the container and installs aiormq via pip, so the wire-capture tests run with zero manual setup. Verified locally: `interop-messaging-core` 22/22 green against live brokers (exactly the CI command with `-DskipTests=true`), empty-group pass-through and typo-guard fail-fast both confirmed.
