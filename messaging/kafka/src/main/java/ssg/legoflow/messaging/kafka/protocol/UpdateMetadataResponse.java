package ssg.legoflow.messaging.kafka.protocol;

/**
 * UpdateMetadata response (API key 6).
 *
 * @param errorCode the error code
 * @since 0.1.0
 */
public record UpdateMetadataResponse(short errorCode) {
}
