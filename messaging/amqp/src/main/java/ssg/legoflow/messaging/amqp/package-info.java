/**
 * AMQP 1.0 (ISO 19464) messaging protocol implementation.
 *
 * <p>Provides a complete AMQP 1.0 stack with invariant core (transport-agnostic)
 * and TCP adapter. Includes container (server), client, AMQP type system codec,
 * session multiplexing, credit-based link flow control, SASL authentication,
 * and delivery guarantee semantics (at-most-once, at-least-once, exactly-once).
 *
 * @since 0.1.0
 */
package ssg.legoflow.messaging.amqp;
