/**
 * STOMP 1.2 (Simple Text Oriented Messaging Protocol) implementation.
 *
 * <p>Provides a composable STOMP stack with transport-agnostic invariant core
 * and pluggable transport adapters (TCP, WebSocket). Includes both broker and
 * client implementations with full support for subscriptions, transactions,
 * acknowledgment modes, heart-beats, and receipts.
 *
 * @since 0.1.0
 */
package ssg.legoflow.messaging.stomp;
